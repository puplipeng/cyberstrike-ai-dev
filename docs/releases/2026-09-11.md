# 2026-09-11 源码更新

- 新增 APK 反编译与整包代码审计工作区，包含 8 类规则、覆盖缺口、报告/源码导出与分批 AI 审计草稿，见 [APK 审计说明](../APK_AUDIT.md)。
- 提高流式工具调用片段容量，仍保留工具数量、参数大小和异常空片段保护。
- 优化项目对话目录加载：复用请求，优先显示项目列表。
- 同步资产优先级筛选、GitHub 监控规则折叠与漏洞入库过滤。
- Windows 服务由按需计划任务持有，减少临时启动终端退出导致的服务中断；不会自动恢复扫描任务。

仅发布通用源码、测试及文档。配置、数据库、真实凭据、扫描报告、APK、SSH 数据、个人知识库与运行目录均不上传。保留此前公开分支的 PostgreSQL/Linux 兼容和合成凭据测试修复，不用本机配置覆盖公开示例。

APK 整包结果为静态候选，不保证发现全部漏洞；AI 草稿仍需用户审核后发送，不自动上传代码到模型。
